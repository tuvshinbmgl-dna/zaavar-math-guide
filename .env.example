# Copy to .env (or just export in your shell) and fill in your Anthropic API key.
# The app reads ANTHROPIC_API_KEY at runtime. Without it, the guide/assessment
# still work fully; only the AI tutor chat is disabled (shows a friendly notice).
ANTHROPIC_API_KEY=sk-ant-...
